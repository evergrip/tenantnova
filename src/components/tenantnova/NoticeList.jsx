import React from "react";

export default function NoticeList({ notices, lookups = {}, showInternal = false, onEdit, onArchive, onAcknowledge }) {
  if (!notices.length) return <div className="rounded-2xl border bg-white p-6 text-slate-500">No notices or forms found.</div>;
  return <div className="grid gap-4">{notices.map(n => <div key={n.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm text-slate-500">{n.notice_type} · {n.form_code_optional || "No form code"}</p><h3 className="text-lg font-bold">{n.title}</h3><p className="mt-2 whitespace-pre-wrap text-slate-700">{n.body}</p></div><span className="h-fit rounded-full bg-teal-100 px-3 py-1 text-sm text-teal-800">{n.status}</span></div><dl className="mt-4 grid gap-3 text-sm md:grid-cols-4"><div><dt className="text-slate-500">Served</dt><dd className="font-medium">{n.served_date || "Not served"}</dd></div><div><dt className="text-slate-500">Due</dt><dd className="font-medium">{n.due_date || "No due date"}</dd></div><div><dt className="text-slate-500">Delivery</dt><dd className="font-medium">{n.delivery_method}</dd></div><div><dt className="text-slate-500">Property</dt><dd className="font-medium">{lookups.properties?.[n.property_id_nullable]?.property_name || "—"}</dd></div></dl>{n.tenant_visible_note && <p className="mt-4 rounded-xl bg-slate-50 p-3 text-sm">{n.tenant_visible_note}</p>}{n.file_url_or_storage_reference && <p className="mt-3 text-sm text-slate-500">File reference: {n.file_url_or_storage_reference}</p>}{showInternal && <p className="mt-3 text-sm text-slate-500">Internal: {n.internal_admin_note || "None"}</p>}<div className="mt-4 flex flex-wrap gap-2">{onAcknowledge && n.requires_acknowledgement !== false && n.status === "Served" && <button onClick={() => onAcknowledge(n)} className="rounded-xl bg-teal-700 px-3 py-2 text-sm text-white">Acknowledge</button>}{showInternal && <button onClick={() => onEdit?.(n)} className="rounded-xl bg-slate-900 px-3 py-2 text-sm text-white">Edit</button>}{showInternal && <button onClick={() => onArchive?.(n)} className="rounded-xl border px-3 py-2 text-sm text-slate-600">Archive</button>}</div></div>)}</div>;
}