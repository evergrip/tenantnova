import React from "react";
import { deliveryMethods, noticeStatuses, noticeTypes } from "@/lib/notices";

export default function NoticeForm({ form, setForm, onSubmit, properties = [], units = [], leases = [], tenants = [] }) {
  const set = (k, v) => setForm({ ...form, [k]: v });
  return <form onSubmit={onSubmit} className="rounded-2xl border bg-white p-5"><div className="grid gap-3 md:grid-cols-2"><input required className="rounded-xl border p-3 md:col-span-2" placeholder="Notice/Form title" value={form.title || ""} onChange={e => set("title", e.target.value)} /><textarea required className="rounded-xl border p-3 md:col-span-2" placeholder="Notice body or form instructions" value={form.body || ""} onChange={e => set("body", e.target.value)} /><select className="rounded-xl border p-3" value={form.notice_type || "General Notice"} onChange={e => set("notice_type", e.target.value)}>{noticeTypes.map(v => <option key={v}>{v}</option>)}</select><select className="rounded-xl border p-3" value={form.status || "Draft"} onChange={e => set("status", e.target.value)}>{noticeStatuses.map(v => <option key={v}>{v}</option>)}</select><select className="rounded-xl border p-3" value={form.delivery_method || "Portal"} onChange={e => set("delivery_method", e.target.value)}>{deliveryMethods.map(v => <option key={v}>{v}</option>)}</select><input className="rounded-xl border p-3" placeholder="NS form code, if any" value={form.form_code_optional || ""} onChange={e => set("form_code_optional", e.target.value)} /><select className="rounded-xl border p-3" value={form.property_id_nullable || ""} onChange={e => set("property_id_nullable", e.target.value)}><option value="">Property optional</option>{properties.map(p => <option key={p.id} value={p.id}>{p.property_name}</option>)}</select><select className="rounded-xl border p-3" value={form.unit_id_nullable || ""} onChange={e => set("unit_id_nullable", e.target.value)}><option value="">Unit optional</option>{units.map(u => <option key={u.id} value={u.id}>{u.unit_number}</option>)}</select><select className="rounded-xl border p-3" value={form.lease_id_nullable || ""} onChange={e => set("lease_id_nullable", e.target.value)}><option value="">Lease optional</option>{leases.map(l => <option key={l.id} value={l.id}>{l.start_date} · {l.lease_status}</option>)}</select><select className="rounded-xl border p-3" value={form.tenant_id_nullable || ""} onChange={e => set("tenant_id_nullable", e.target.value)}><option value="">Tenant optional</option>{tenants.map(t => <option key={t.id} value={t.id}>{t.first_name} {t.last_name}</option>)}</select><input type="date" className="rounded-xl border p-3" value={form.served_date || ""} onChange={e => set("served_date", e.target.value)} /><input type="date" className="rounded-xl border p-3" value={form.due_date || ""} onChange={e => set("due_date", e.target.value)} /><label className="flex items-center gap-2 rounded-xl border p-3"><input type="checkbox" checked={form.requires_acknowledgement !== false} onChange={e => set("requires_acknowledgement", e.target.checked)} /> Requires acknowledgement</label><input className="rounded-xl border p-3" placeholder="Dummy file reference" value={form.file_url_or_storage_reference || ""} onChange={e => set("file_url_or_storage_reference", e.target.value)} /><textarea className="rounded-xl border p-3 md:col-span-2" placeholder="Tenant-visible note" value={form.tenant_visible_note || ""} onChange={e => set("tenant_visible_note", e.target.value)} /><textarea className="rounded-xl border p-3 md:col-span-2" placeholder="Internal admin note" value={form.internal_admin_note || ""} onChange={e => set("internal_admin_note", e.target.value)} /></div><button className="mt-4 rounded-xl bg-teal-700 px-4 py-2 text-white">Save notice/form</button></form>;
}