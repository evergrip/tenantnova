import React from "react";

export default function ContactMessageList({ messages, lookups = {}, showInternal = false, onRespond, onStatus, onArchive }) {
  if (!messages.length) return <div className="rounded-2xl border bg-white p-6 text-slate-500">No contact messages found.</div>;
  return <div className="grid gap-4">{messages.map(m => <div key={m.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm text-slate-500">{m.category} · {m.priority} priority</p><h3 className="text-lg font-bold">{m.subject}</h3><p className="mt-2 whitespace-pre-wrap text-slate-700">{m.body}</p></div><span className="h-fit rounded-full bg-teal-100 px-3 py-1 text-sm text-teal-800">{m.status}</span></div><dl className="mt-4 grid gap-3 text-sm md:grid-cols-4"><div><dt className="text-slate-500">Preferred contact</dt><dd className="font-medium">{m.preferred_contact_method}</dd></div><div><dt className="text-slate-500">Tenant</dt><dd className="font-medium">{lookups.tenants?.[m.tenant_id] ? `${lookups.tenants[m.tenant_id].first_name} ${lookups.tenants[m.tenant_id].last_name}` : "—"}</dd></div><div><dt className="text-slate-500">Property</dt><dd className="font-medium">{lookups.properties?.[m.property_id_nullable]?.property_name || "—"}</dd></div><div><dt className="text-slate-500">Unit</dt><dd className="font-medium">{lookups.units?.[m.unit_id_nullable]?.unit_number || "—"}</dd></div></dl>{m.admin_response && <div className="mt-4 rounded-xl bg-slate-50 p-3 text-sm"><b>Admin response</b><p className="mt-1 whitespace-pre-wrap">{m.admin_response}</p></div>}{showInternal && <p className="mt-3 text-sm text-slate-500">Internal: {m.internal_admin_note || "None"}</p>}<div className="mt-4 flex flex-wrap gap-2">{showInternal && <button onClick={() => onRespond?.(m)} className="rounded-xl bg-teal-700 px-3 py-2 text-sm text-white">Respond</button>}{showInternal && <button onClick={() => onStatus?.(m)} className="rounded-xl bg-slate-900 px-3 py-2 text-sm text-white">Update status</button>}{showInternal && <button onClick={() => onArchive?.(m)} className="rounded-xl border px-3 py-2 text-sm text-slate-600">Archive</button>}</div></div>)}</div>;
}