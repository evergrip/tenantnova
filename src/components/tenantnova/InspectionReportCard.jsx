import React from "react";

export default function InspectionReportCard({ report, admin = false, lookups = {}, onEdit, onArchive }) {
  return <div className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm text-slate-500">{report.inspection_type} · {report.inspection_date}</p><h3 className="text-lg font-bold">Inspection report</h3></div><span className={`h-fit rounded-full px-3 py-1 text-sm ${report.shared_with_tenant ? "bg-teal-100 text-teal-800" : "bg-slate-100 text-slate-600"}`}>{report.shared_with_tenant ? "Shared" : "Not shared"}</span></div><dl className="mt-4 grid gap-3 text-sm md:grid-cols-4"><div><dt className="text-slate-500">Property</dt><dd>{lookups.properties?.[report.property_id]?.property_name || report.property_id}</dd></div><div><dt className="text-slate-500">Unit</dt><dd>{lookups.units?.[report.unit_id]?.unit_number || report.unit_id}</dd></div><div><dt className="text-slate-500">Tenant present</dt><dd>{report.tenant_present ? "Yes" : "No"}</dd></div><div><dt className="text-slate-500">Signatures</dt><dd>{report.tenant_signature_placeholder || "Tenant pending"} / {report.admin_signature_placeholder || "Admin pending"}</dd></div></dl>{report.tenant_visible_notes && <p className="mt-4 rounded-xl bg-teal-50 p-3 text-sm text-teal-900">{report.tenant_visible_notes}</p>}<pre className="mt-3 overflow-auto rounded-xl bg-slate-50 p-3 text-xs">{JSON.stringify(report.room_by_room_condition_json || {}, null, 2)}</pre>{report.photos_array?.length > 0 && <p className="mt-3 text-sm text-slate-600">Photo refs: {report.photos_array.join(", ")}</p>}{admin && <pre className="mt-3 overflow-auto rounded-xl bg-amber-50 p-3 text-xs">Damages: {JSON.stringify(report.damages_json || {}, null, 2)}</pre>}{admin && <p className="mt-3 text-sm text-slate-500">Internal: {report.internal_admin_notes || "—"}</p>}<div className="mt-4 flex gap-2">{admin && <button onClick={() => onEdit?.(report)} className="rounded-xl bg-slate-900 px-3 py-2 text-sm text-white">Edit</button>}{admin && <button onClick={() => onArchive?.(report)} className="rounded-xl border px-3 py-2 text-sm">Archive</button>}</div></div>;
}